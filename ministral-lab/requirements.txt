# Unsloth Training Requirements
# Install with: pip install -r requirements.txt

# Unsloth (install first)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Core dependencies (install with --no-deps after unsloth)
trl>=0.9.0
peft>=0.12.0
accelerate>=0.30.0
bitsandbytes>=0.43.0

# Data handling
datasets>=2.19.0
transformers>=4.43.0

# Utils
torch>=2.4.0
safetensors>=0.4.0